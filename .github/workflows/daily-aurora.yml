name: Daily Aurora Update

on:
  schedule:
    # Run daily at 10:00 AM China time (02:00 UTC)
    - cron: '0 2 * * *'
  workflow_dispatch:  # Allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: pip install googletrans==4.0.0-rc1 || pip install translate || true

      - name: Run daily aurora update script
        run: python3 .github/scripts/aurora-daily.py

      - name: Run flight price tracker
        run: python3 .github/scripts/flight-tracker.py

      - name: Commit and push if changed
        run: |
          git config user.name "aurora-bot"
          git config user.email "aurora-bot@users.noreply.github.com"
          git add norway-travel/norway-news-data.json norway-travel/travel_plan_v3.html flight-tracker/flight-prices.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "daily: update aurora forecast & flight prices $(date +'%Y-%m-%d')"
            git push
          fi
